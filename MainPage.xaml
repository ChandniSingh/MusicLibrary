<Page
    x:Class="MusicApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid><Image Source="uri" />
        
        <GridView Name="Albums" ItemsSource="{Binding}" Height="300" Width="400" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionChanged="Albums_SelectionChanged">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:Album">
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{x:Bind AlbumName}" Width="400">
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <Button Content="Add Music" Click="Button_Click"/>
        <Button Content="Play" Click="Play_Click" Margin="619,778,0,0" VerticalAlignment="Top"/>
        <Image HorizontalAlignment="Left" Height="100" Margin="490,698,0,0" VerticalAlignment="Top" Width="100"/>
    </Grid>
</Page>
